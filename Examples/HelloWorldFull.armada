namespace ArmadaApp1;

class Program;

    has private @world:string = "World";

    #entrypoint 
    fn private Main of args:string[] -> void {
        let hello = "hello",
        writeln @hello + @world
    };

    has private @newArray:Array<string> = ["1" | "2" | "3" | "4" | "My Name is Bob and I live in a dojo"];
    has private @objectExample:Foo = new  [ Bar = 1 | FooBar = "foobar" ];